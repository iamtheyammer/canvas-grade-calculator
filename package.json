{
  "name": "canvas-grade-calculator-heroku",
  "version": "0.0.0",
  "description": "this is here because heroku requires a package.json in the root dir",
  "dependencies": {},
  "scripts": {
    "build": "make heroku"
  }
}
